# 🧠 Memory Agent

The Memory Agent provides long-term state for the AI automation system.

It stores and retrieves structured information generated by other agents
(e.g. summaries, actions taken, decisions made).

---

## ✨ Responsibilities

- Persist execution results
- Retrieve historical context
- Enable continuity across agent runs
- Act as a system-level memory layer

---

## 🏗 Architecture

- Local JSON-based storage (v1)
- Deterministic, model-free design
- Designed for future vector DB upgrades

---

## 🚀 Example Usage

```bash
python3 src/main.py